header.header
  nav.nav.nav--tours
    a.nav__el(href='/modern') Natours
  .header__logo
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Çıkış
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`${user.name} fotoğrafı`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Giriş Yap
      a.nav__el.nav__el--cta(href='/signup') Kaydol
